import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const metadata: Metadata = {
  title: "AtlasVault AI — Company knowledge, compiled into winning proposals",
  description:
    "AtlasVault AI turns scattered company documents into a living knowledge base that generates evidence-backed proposals, tender responses and compliance answers.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans">{children}</body>
    </html>
  );
}
